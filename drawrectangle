#include<stdlib.h>
#include <stdio.h>
#include<time.h>
void drawarectangle(){
    srand(time(NULL));
    int height=rand()%5+1;
    int width =rand()%10+1;
   
    for(int i=0;i<height; i++){
        for(int j=0;j<width; j++){
            printf("*");
        }
        printf("\n");
    }
}

int main() {
    drawarectangle();

    return 0;
}
